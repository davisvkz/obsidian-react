// Empty stub of the obsidian module for unit tests.
// markdownStore.ts only does `import type` from obsidian (erased by the compiler),
// but this file prevents an accidental runtime import from breaking tests.
export {};
